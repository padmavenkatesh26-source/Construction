# GitHub Pages Construction Website (Ready)

## How to use
1) Replace "YOUR COMPANY NAME" and contact details in each HTML file (search/replace).
2) (Optional) Replace placeholder SVGs in assets/img with real photos (same filenames or update HTML).
3) Upload to GitHub repo root.
4) Enable GitHub Pages: Settings → Pages → Deploy from branch → main → /(root).
